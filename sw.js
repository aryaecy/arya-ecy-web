const CACHE='arya-ecy-v3.4.0';
const CORE=['/','/hakkimizda','/hizmetler','/kutuphane','/mevzuat','/iletisim','/kariyer','/duyurular','/haberler','/offline.html','/logo.png','/arya-logo.png','/icon-192.png','/icon-512.png','/hero-integrated-environment.png','/manifest.webmanifest'];
self.addEventListener('install',event=>{event.waitUntil(caches.open(CACHE).then(async c=>{await Promise.allSettled(CORE.map(u=>c.add(u)));}).then(()=>self.skipWaiting()))});
self.addEventListener('activate',event=>{event.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(k=>k.startsWith('arya-ecy-')&&k!==CACHE).map(k=>caches.delete(k)))).then(()=>self.clients.claim()))});
self.addEventListener('fetch',event=>{const req=event.request;if(req.method!=='GET')return;const url=new URL(req.url);if(url.origin!==location.origin)return;if(req.mode==='navigate'){event.respondWith(fetch(req,{cache:'no-store'}).then(res=>{if(res.ok){const copy=res.clone();caches.open(CACHE).then(c=>c.put(req,copy))}return res}).catch(async()=>await caches.match(req)||await caches.match('/')||await caches.match('/offline.html')));return;}event.respondWith(caches.match(req).then(cached=>cached||fetch(req).then(res=>{if(res.ok){const copy=res.clone();caches.open(CACHE).then(c=>c.put(req,copy))}return res}).catch(()=>new Response('Çevrimdışı ve kaynak önbellekte değil.',{status:503,headers:{'Content-Type':'text/plain; charset=utf-8'}}))))});
