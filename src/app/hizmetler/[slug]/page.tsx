import Navbar from '@/components/Navbar';import {services} from '@/lib/siteData';import {notFound} from 'next/navigation';
export function generateStaticParams(){return services.map(s=>({slug:s.slug}))}
export default async function ServicePage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=services.find(x=>x.slug===slug);if(!s)notFound();const Icon=s.icon;return <main className="article-page"><Navbar/><section className="service-detail"><div className="service-detail-copy"><span>ARYA ECY • Hizmetler</span><Icon size={38}/><h1>{s.title}</h1><p className="lead">{s.intro}</p>{s.sections.map(([h,p])=><div key={h}><h2>{h}</h2><p>{p}</p></div>)}<div className="detail-note"><b>ARYA ECY yaklaşımı</b><p>Her hizmeti tesisin faaliyet yapısına göre özelleştirir; mevzuat, saha uygulaması, kayıt düzeni ve iyileştirme çalışmalarını aynı plan içinde yürütürüz. Böylece süreçler yalnızca tamamlanmış değil, izlenebilir ve denetime hazır hale gelir.</p></div></div><div className="service-detail-visual"><img src="/environment-hero.png" alt={`${s.title} hizmeti - ARYA ECY`}/><div><b>{s.title}</b><span>Uzman ekip • saha uygulaması • mevzuat uyumu • sürekli iyileştirme</span></div></div></section></main>}
