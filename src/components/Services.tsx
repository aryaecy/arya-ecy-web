import Link from 'next/link'; import {services} from '@/lib/siteData';
export default function Services(){return <section id="hizmetler" className="services-panel"><div className="section-heading"><span>Hizmetlerimiz</span><h2>Sahada, mevzuatta ve sürdürülebilirlikte bütüncül çözüm</h2></div><div className="service-grid">{services.map(s=>{const Icon=s.icon;return <Link key={s.slug} href={`/hizmetler/${s.slug}`} className="service-card"><Icon size={23}/><div><h3>{s.title}</h3><p>{s.summary}</p><b>Detaylı bilgi →</b></div></Link>})}</div></section>}
